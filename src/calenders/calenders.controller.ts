import { Controller } from '@nestjs/common';

@Controller('calenders')
export class CalendersController {

    //user List of all appointment

    //user Creare a new appointment  (date, title for appoint , date + time to notification ) as in-progress

    //notifiction and update appointment at Done

    //user get all appointment Done

    //user get list all appointment in-progress

    //user delete appointment  ( in-progress)
}
